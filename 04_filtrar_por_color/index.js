/**
 * Ejercicio: Filtrar usuarios por color favorito
 * 
 * En este ejercicio, debes completar la función `filtrarPorColorFavorito` que toma 
 * como parámetros un arreglo de objetos que representan usuarios y un string que 
 * representa el color favorito. Cada objeto usuario tiene una propiedad `colorFavorito`. 
 * La función debe devolver un nuevo arreglo con solo aquellos usuarios cuyo color 
 * favorito coincida con el color pasado como segundo parámetro. Si no hay coincidencias, 
 * debe retornar un arreglo vacío.
 * 
 * Ejemplo de uso:
 * 
 * const usuarios = [
 *   { nombre: "Juan", colorFavorito: "azul" },
 *   { nombre: "Ana", colorFavorito: "rojo" },
 *   { nombre: "Pedro", colorFavorito: "azul" }
 * ];
 * 
 * filtrarPorColorFavorito(usuarios, "azul"); 
 * // Debería retornar: [{ nombre: "Juan", colorFavorito: "azul" }, { nombre: "Pedro", colorFavorito: "azul" }]
 */

function filtrarPorColorFavorito(usuarios, colorFavorito) {
    // Tu código aquí
}

// Exportar la función para pruebas
module.exports = filtrarPorColorFavorito;
