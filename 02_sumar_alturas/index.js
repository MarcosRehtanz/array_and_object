/**
 * Ejercicio: Sumar alturas de usuarios
 * 
 * En este ejercicio, debes completar la función `sumarAlturas` que toma como 
 * parámetro un arreglo de objetos que representan usuarios. Cada objeto tiene 
 * una propiedad `altura` que es un número. La función debe sumar todas las alturas 
 * de los usuarios y devolver el total. Si el arreglo está vacío, debe retornar 0.
 * 
 * Ejemplo de uso:
 * 
 * const usuarios = [
 *   { nombre: "Juan", altura: 180 },
 *   { nombre: "Ana", altura: 165 },
 *   { nombre: "Pedro", altura: 175 }
 * ];
 * 
 * sumarAlturas(usuarios); 
 * // Debería retornar: 520
 */

function sumarAlturas(usuarios) {
    // Tu código aquí
}

// Exportar la función para pruebas
module.exports = sumarAlturas;
