/**
 * Ejercicio: Filtrar objetos en un arreglo
 * 
 * En este ejercicio, debes completar la función `filtrarUsuarios` que toma como 
 * primer parámetro un arreglo de objetos y un segundo parámetro opcional que 
 * representa una propiedad específica a filtrar. La función debe devolver un 
 * nuevo arreglo que contenga solo los objetos que tienen la propiedad especificada 
 * con un valor verdadero (true). Si no se proporciona el segundo parámetro, 
 * la función debe devolver todos los objetos del arreglo que tienen la propiedad 
 * `activo` establecida en true.
 * 
 * Ejemplo de uso:
 * 
 * const usuarios = [
 *   { nombre: "Juan", activo: true },
 *   { nombre: "Ana", activo: false },
 *   { nombre: "Pedro", activo: true }
 * ];
 * 
 * filtrarUsuarios(usuarios); 
 * // Debería retornar: [{ nombre: "Juan", activo: true }, { nombre: "Pedro", activo: true }]
 */

function filtrarUsuarios(usuarios) {
    // Tu código aquí
}

// Exportar la función para pruebas
module.exports = filtrarUsuarios;
