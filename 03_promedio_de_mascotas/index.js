/**
 * Ejercicio: Calcular el promedio de mascotas por usuario
 * 
 * En este ejercicio, debes completar la función `promedioMascotas` que toma como 
 * parámetro un arreglo de objetos que representan usuarios. Cada objeto tiene 
 * una propiedad `mascotas`, que es un número que representa la cantidad de mascotas 
 * que tiene el usuario. La función debe calcular el promedio de mascotas en el 
 * grupo y devolverlo. Si el arreglo está vacío, debe retornar 0.
 * 
 * Ejemplo de uso:
 * 
 * const usuarios = [
 *   { nombre: "Juan", mascotas: 2 },
 *   { nombre: "Ana", mascotas: 3 },
 *   { nombre: "Pedro", mascotas: 5 }
 * ];
 * 
 * promedioMascotas(usuarios); 
 * // Debería retornar: 3.33 (promedio de 2, 3 y 5)
 */

function promedioMascotas(usuarios) {
    // Tu código aquí
}

// Exportar la función para pruebas
module.exports = promedioMascotas;
